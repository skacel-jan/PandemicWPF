<UserControl
    x:Class="Pandemic.Views.MoveSelection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Pandemic.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <ItemsControl Background="Black" ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button
                        Background="Black"
                        BorderBrush="{StaticResource ActionEnabledBrush}"
                        BorderThickness="2"
                        Command="{Binding DataContext.SelectedCommand, Mode=OneWay, ElementName=root}"
                        CommandParameter="{Binding}"
                        Content="{Binding MoveType}"
                        FontSize="18"
                        Foreground="White" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
