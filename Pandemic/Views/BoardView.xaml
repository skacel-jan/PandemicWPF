<UserControl
    x:Class="Pandemic.Views.BoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
    xmlns:converters="clr-namespace:Pandemic.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:default="clr-namespace:Pandemic"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:Pandemic.Views"
    xmlns:viewModels="clr-namespace:Pandemic.ViewModels"
    x:Name="Board"
    d:DesignHeight="800"
    d:DesignWidth="1400"
    FontFamily="Calibri"
    mc:Ignorable="d">
    <UserControl.Resources>

        <DataTemplate DataType="{x:Type viewModels:DiseaseSelectionViewModel}">
            <view:DiseaseSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:ShareTypeSelectionViewModel}">
            <view:ShareTypeSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:CharacterSelectionViewModel}">
            <view:CharacterSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:CardsSelectionViewModel}">
            <view:CardSelection>
                <!--<view:CardSelection.Triggers>
                    <EventTrigger RoutedEvent="view:CardSelection.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    AccelerationRatio=".1"
                                    Storyboard.TargetProperty="MaxWidth"
                                    From="10"
                                    To="{Binding ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentControl}}}"
                                    Duration="0:0:.8" />
                                <ThicknessAnimation
                                    AccelerationRatio=".1"
                                    Storyboard.Target="{x:Reference InfoBorder}"
                                    Storyboard.TargetProperty="Margin"
                                    From="0,100,0,-100"
                                    Duration="0:0:.8" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </view:CardSelection.Triggers>-->
            </view:CardSelection>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:CardsViewModel}">
            <view:CardsView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MoveSelectionViewModel}">
            <view:MoveSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:TextViewModel}">
            <view:InfoTextView />
        </DataTemplate>

        <Style x:Key="PileBorder" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="Margin" Value="1" />
        </Style>

        <Style x:Key="PileLabel" TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

    </UserControl.Resources>

    <Grid>
        <DockPanel
            Grid.RowSpan="3"
            Grid.ColumnSpan="3"
            Background="Black">

            <Label
                HorizontalAlignment="Stretch"
                VerticalAlignment="Bottom"
                HorizontalContentAlignment="Center"
                Content="{Binding InfoText, FallbackValue=Info}"
                DockPanel.Dock="Bottom"
                FontSize="14"
                Foreground="White" />
            <Grid
                Width="1400"
                Panel.ZIndex="1"
                DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="0.15*" />
                    <ColumnDefinition Width="0.15*" />
                </Grid.ColumnDefinitions>
                <Grid
                    Grid.ColumnSpan="3"
                    HorizontalAlignment="Center"
                    Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                    </Grid.ColumnDefinitions>
                    <Button
                        Grid.Column="0"
                        Command="{Binding MoveActionCommand}"
                        Content="Move"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="1"
                        Command="{Binding TreatActionCommand}"
                        Content="Treat"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="2"
                        Command="{Binding ShareActionCommand}"
                        Content="Share"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="3"
                        Command="{Binding BuildActionCommand}"
                        Content="Build"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="4"
                        Command="{Binding DiscoverCureActionCommand}"
                        Content="Find"
                        Style="{StaticResource ActionButtonStyle}" />

                    <Button
                        Grid.Column="5"
                        Background="Black"
                        Command="{Binding CancelCommand}"
                        Content="Cancel"
                        Foreground="Red"
                        IsCancel="True" />
                    <Button
                        Grid.Column="6"
                        Background="Black"
                        Command="{Binding EventsCommand}"
                        Content="Events"
                        Foreground="Yellow" />
                </Grid>
                <Label
                    Margin="163,0,327,29"
                    Content="{Binding Board.GameData.InfectionRate, FallbackValue=2}"
                    ContentStringFormat="Infection rate: {0}"
                    Style="{StaticResource PileLabel}" />
                <Label
                    Margin="163,32,349,0"
                    Content="{Binding Board.GameData.Outbreaks, FallbackValue=0}"
                    ContentStringFormat="Outbreaks: {0}"
                    Style="{StaticResource PileLabel}" />
                <Grid Grid.Column="0" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="14*" />
                        <RowDefinition Height="17*" />
                    </Grid.RowDefinitions>
                    <Label
                        Grid.ColumnSpan="4"
                        Content="Cure markers"
                        Style="{StaticResource PileLabel}" />
                    <Border Grid.Row="1" BorderBrush="DarkGray">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Black].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        BorderBrush="Blue">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Blue].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="2"
                        BorderBrush="Red">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Red].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="3"
                        BorderBrush="Yellow">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Yellow].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </Grid>
                <Grid
                    Grid.Column="2"
                    Margin="0,0,100,0"
                    HorizontalAlignment="Right">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="14*" />
                        <RowDefinition Height="17*" />
                    </Grid.RowDefinitions>
                    <Label
                        Grid.ColumnSpan="4"
                        Content="Cube piles"
                        Style="{StaticResource PileLabel}" />
                    <Border
                        Grid.Row="1"
                        BorderBrush="DarkGray"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Black].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        BorderBrush="Blue"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Blue].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="2"
                        BorderBrush="Red"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Red].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="3"
                        BorderBrush="Yellow"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.GameData.Diseases[(default:DiseaseColor)Yellow].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                </Grid>
                <view:PlayingCard
                    Grid.Column="3"
                    Height="50"
                    CardName="{Binding}"
                    Command="{Binding DataContext.PlayerDiscardPileCommand, ElementName=Board}"
                    DataContext="{Binding Board.PlayerDiscardPile.TopCard}" />
                <view:PlayingCard
                    Grid.Column="4"
                    Height="50"
                    CardName="{Binding}"
                    Command="{Binding DataContext.InfectionDiscardPileCommand, ElementName=Board}"
                    DataContext="{Binding Board.InfectionDiscardPile.TopCard}" >
                    <view:PlayingCard.Background>
                        <RadialGradientBrush>
                            <GradientStop Color="Black"/>
                            <GradientStop Color="White" Offset="1"/>
                        </RadialGradientBrush>
                    </view:PlayingCard.Background>
                </view:PlayingCard>
            </Grid>
            <Grid HorizontalAlignment="Center" Panel.ZIndex="10">

                <Grid.Resources>
                    <CollectionViewSource x:Key="playerCards" Source="{Binding CurrentCharacter.Cards}">
                        <CollectionViewSource.SortDescriptions>
                            <componentmodel:SortDescription Direction="Ascending" />
                        </CollectionViewSource.SortDescriptions>
                    </CollectionViewSource>
                </Grid.Resources>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer
                    Grid.RowSpan="2"
                    Width="1400"
                    default:TouchScrolling.IsEnabled="True"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Hidden">
                    <StackPanel Width="2800" Orientation="Horizontal">
                        <view:WorldMap Width="1400" Margin="0,-80,0,0" />
                        <view:WorldMap Width="1400" Margin="0,-80,0,0" />
                    </StackPanel>
                </ScrollViewer>
                <ItemsControl
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    ItemsSource="{Binding Source={StaticResource playerCards}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <view:PlayingCard CardName="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </DockPanel>
        <Grid>
            <Thumb
                Margin="10,450,10,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                DragDelta="Thumb_DragDelta"
                Visibility="{Binding IsInfoVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Thumb.Template>
                    <ControlTemplate>
                        <ContentControl
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{Binding InfoViewModel}" />
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb
                Margin="10,550,10,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                DragDelta="Thumb_DragDelta"
                Visibility="{Binding IsActionVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Thumb.Template>
                    <ControlTemplate>
                        <ContentControl
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{Binding ActionViewModel}" />
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>

        </Grid>
    </Grid>
</UserControl>
