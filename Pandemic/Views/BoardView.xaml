<UserControl
    x:Class="Pandemic.Views.BoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Pandemic.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:default="clr-namespace:Pandemic"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:Pandemic.Views"
    xmlns:viewModels="clr-namespace:Pandemic.ViewModels"
    x:Name="Board"
    d:DesignHeight="800"
    d:DesignWidth="1400"
    FontFamily="Calibri"
    mc:Ignorable="d">
    <UserControl.Resources>

        <converters:DiseaseToColorConverter x:Key="DiseaseToColorConverter" />

        <DataTemplate DataType="{x:Type viewModels:DiseaseSelectionViewModel}">
            <view:DiseaseSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:CardSelectionViewModel}">
            <view:CardSelection>
                <!--<view:CardSelection.Triggers>
                    <EventTrigger RoutedEvent="view:CardSelection.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation AccelerationRatio=".1"
                                                 Storyboard.TargetProperty="MaxWidth"
                                                 From="10"
                                                 To="{Binding ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentControl}}}"
                                                 Duration="0:0:.8" />
                                <ThicknessAnimation AccelerationRatio=".1"
                                                    Storyboard.Target="{x:Reference InfoBorder}"
                                                    Storyboard.TargetProperty="Margin"
                                                    From="0,100,0,-100"
                                                    Duration="0:0:.8" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </view:CardSelection.Triggers>-->
            </view:CardSelection>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MultiCardsSelectionViewModel}">
            <view:CardSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MoveSelectionViewModel}">
            <view:MoveSelection />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:TextViewModel}">
            <view:InfoTextView />
        </DataTemplate>

        <Style x:Key="PileBorder" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="Margin" Value="1" />
        </Style>

        <Style x:Key="PileLabel" TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

    </UserControl.Resources>

    <Grid>

        <DockPanel
            Grid.RowSpan="3"
            Grid.ColumnSpan="3"
            Background="Black">

            <Label
                HorizontalAlignment="Stretch"
                VerticalAlignment="Bottom"
                HorizontalContentAlignment="Center"
                Content="{Binding InfoText, FallbackValue=Info}"
                DockPanel.Dock="Bottom"
                FontSize="14"
                Foreground="White" />
            <Grid Width="1400" DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="0.15*" />
                    <ColumnDefinition Width="0.15*" />
                </Grid.ColumnDefinitions>
                <Grid
                    Grid.ColumnSpan="3"
                    HorizontalAlignment="Center"
                    Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                        <ColumnDefinition SharedSizeGroup="A" />
                    </Grid.ColumnDefinitions>
                    <Button
                        Grid.Column="0"
                        Command="{Binding MoveActionCommand}"
                        Content="Move"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="1"
                        Command="{Binding TreatActionCommand}"
                        Content="Treat"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="2"
                        Content="Share"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="3"
                        Command="{Binding BuildActionCommand}"
                        Content="Build"
                        Style="{StaticResource ActionButtonStyle}" />
                    <Button
                        Grid.Column="4"
                        Command="{Binding DiscoverCureActionCommand}"
                        Content="Find"
                        Style="{StaticResource ActionButtonStyle}" />

                    <Button
                        Grid.Column="5"
                        Width="90"
                        HorizontalAlignment="Left"
                        Background="Black"
                        Command="{Binding CancelCommand}"
                        Content="Cancel"
                        Foreground="Red"
                        IsCancel="True" />
                </Grid>
                <Label
                    Margin="163,0,327,29"
                    Content="{Binding Board.InfectionRate, FallbackValue=2}"
                    ContentStringFormat="Infection rate: {0}"
                    Style="{StaticResource PileLabel}" />
                <Label
                    Margin="163,32,349,0"
                    Content="{Binding Board.Outbreaks, FallbackValue=0}"
                    ContentStringFormat="Outbreaks: {0}"
                    Style="{StaticResource PileLabel}" />
                <Grid Grid.Column="0" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="14*" />
                        <RowDefinition Height="17*" />
                    </Grid.RowDefinitions>
                    <Label
                        Grid.ColumnSpan="4"
                        Content="Cure markers"
                        Style="{StaticResource PileLabel}" />
                    <Border Grid.Row="1" BorderBrush="DarkGray">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.Diseases[(default:DiseaseColor)Black].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        BorderBrush="Blue">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.Diseases[(default:DiseaseColor)Blue].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="2"
                        BorderBrush="Red">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.Diseases[(default:DiseaseColor)Red].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="3"
                        BorderBrush="Yellow">
                        <Border.Style>
                            <Style BasedOn="{StaticResource PileBorder}" TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Board.Diseases[(default:DiseaseColor)Yellow].IsCured}" Value="True">
                                        <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </Grid>
                <Grid
                    Grid.Column="2"
                    Margin="0,0,100,0"
                    HorizontalAlignment="Right">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="14*" />
                        <RowDefinition Height="17*" />
                    </Grid.RowDefinitions>
                    <Label
                        Grid.ColumnSpan="4"
                        Content="Cube piles"
                        Style="{StaticResource PileLabel}" />
                    <Border
                        Grid.Row="1"
                        BorderBrush="DarkGray"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.Diseases[(default:DiseaseColor)Black].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        BorderBrush="Blue"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.Diseases[(default:DiseaseColor)Blue].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="2"
                        BorderBrush="Red"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.Diseases[(default:DiseaseColor)Red].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="3"
                        BorderBrush="Yellow"
                        Style="{StaticResource PileBorder}">
                        <Label Content="{Binding Path=Board.Diseases[(default:DiseaseColor)Yellow].Cubes}" Style="{StaticResource PileLabel}" />
                    </Border>
                </Grid>
                <Button
                    Grid.Column="3"
                    Command="{Binding DiscardPileCommand}"
                    CommandParameter="Player"
                    Content="{Binding LastCardInPlayerDiscardPile, FallbackValue=No cards}" />
                <Button
                    Grid.Column="4"
                    Command="{Binding DiscardPileCommand}"
                    CommandParameter="Infection"
                    Content="{Binding LastCardInInfectionDiscardPile, FallbackValue=No cards}" />
            </Grid>
            <Grid DockPanel.Dock="Bottom">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ItemsControl
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    Background="Black"
                    ItemsSource="{Binding CurrentCharacter.Cards}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button
                                Padding="10"
                                Background="Black"
                                BorderBrush="{Binding City.Color, Converter={StaticResource DiseaseToColorConverter}}"
                                BorderThickness="2"
                                Command="{Binding DataContext.CardCommand, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type view:BoardView}}}"
                                CommandParameter="{Binding}"
                                Content="{Binding City.Name, Mode=OneWay}"
                                FontSize="12"
                                Foreground="White" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

            </Grid>
            <view:WorldMap />
        </DockPanel>
        <Grid>
            <Thumb
                Margin="0,450,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                DragDelta="Thumb_DragDelta"
                Visibility="{Binding IsInfoVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Thumb.Template>
                    <ControlTemplate>
                        <ContentControl
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{Binding InfoViewModel}" />
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>


        </Grid>
    </Grid>
</UserControl>
