<UserControl x:Class="Pandemic.Views.InfoTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Pandemic.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="200"
             d:DesignWidth="600"
             FontWeight="SemiBold"
             mc:Ignorable="d">
    <Grid>
        <Border Margin="15"
                BorderBrush="{StaticResource ActionEnabledBrush}"
                BorderThickness="2">
            <TextBlock Padding="10"
                       Background="Black"
                       FontSize="25"
                       Foreground="White"
                       Opacity="0.8"
                       Text="{Binding Text}" />
        </Border>
        <Button Padding="5,2"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                BorderThickness="2"
                Command="{Binding ContinueCommand}"
                Content="{Binding CommandText, FallbackValue=Continue}"
                FontSize="20"
                Style="{StaticResource ActionButtonStyle}"
                Visibility="{Binding ButtonVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>
