<UserControl
    x:Class="Game.Pandemic.Views.MainMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{Binding Path=MainMenu, Source={StaticResource Locator}}"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            BasedOn="{StaticResource ActionButtonStyle}"
            TargetType="Button">
            <Setter Property="Padding" Value="60,5" />
            <Setter Property="FontSize" Value="50" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{StaticResource WorldMapBackground}" />
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Button
                Margin="20,10"
                Command="{Binding StartGameCommand}"
                Content="New game" />

            <Button
                Margin="20,10"
                Command="{Binding LoadGameCommand}"
                Content="Continue game" />
            <Button
                Margin="20,10"
                Command="{Binding ExitCommand}"
                Content="Exit" />
        </StackPanel>
    </Grid>
</UserControl>